# ── 生产（Vercel）────────────────────────────────
# 识图已迁到同域 Serverless /api/ai/ocr，一般不用再填 VITE_API_URL
# 若仍要指向独立后端，再填：
# VITE_API_URL=https://your-backend.example.com
#
# 必填（Vercel → Environment Variables，不要加 VITE_ 前缀）：
# VISION_PROVIDER=zhipu
# VISION_API_KEY=你的智谱Key

# ── 可选：独立后端地址（本地默认 http://localhost:3001）──
# VITE_API_URL=https://your-project.up.railway.app

# ── Supabase（配置后启用账号系统、云端同步）──
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# ── Stripe（配置后启用付费订阅）──
# 部署 Supabase Edge Functions 后填入价格 ID
# VITE_STRIPE_PRICE_PRO_MONTHLY=price_xxx
# VITE_STRIPE_PRICE_PRO_YEARLY=price_xxx
# VITE_STRIPE_PRICE_TEACHER_MONTHLY=price_xxx
# VITE_STRIPE_PRICE_TEACHER_YEARLY=price_xxx
