---
name: 成本控制
description: 成本控制 — Flash优先，仅推理/数学切换Pro，追踪每次调用成本
---

<!--
📝 中文说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 核心作用：控制AI调用成本，优先使用低成本模型

🔔 何时触发：
   - 每次AI调用时自动生效
   - 需要选择模型时

📌 关键要点：
   1. 优先使用Flash模型（低成本）
   2. 仅在以下情况切换Pro：推理、数学证明、复杂几何解析
   3. 每次调用输出：预估成本、模型选择理由、预估Token用量
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-->

优先使用 Flash。仅在以下情况切换Pro：推理、数学证明、复杂几何解析
其余全部 Flash

每次调用输出：Estimated Cost、Reason For Model Selection、Expected Token Usage
