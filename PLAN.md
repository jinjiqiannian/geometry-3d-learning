# 本机连跑：教学 + 搜题可用

授权：只做数学/物理前端；可改任意数量页面/组件/CSS/课程数据；不改 Supabase/Stripe/认证；不中途提问；每项做完自测 `npm run build`。

## 目标

学生能从首页进两种模式，教学点知识点、搜题贴题目，都落到同一套讲解界面，且点开的每个知识点都能真正开讲。

## 验收

- `/#/teach`：左栏按册展开，数学/物理可切换；点叶子立刻开讲，无「预留挂错题」
- `/#/search`：仅搜索框；提交后界面与教学模型壳一致（返回、专题条、步骤/3D）
- `/#/`：两个入口分别进教学/搜题，文案与路由一致
- `npm run build` 通过

## 顺序

1. [x] 课程目录：去掉挂错引擎的预留叶子；只留能开讲的点
2. [x] 教学左栏：默认展开立体几何册+章；选中态清楚；开讲时移动端可收栏
3. [x] 嵌入壳统一：teach/search 顶栏、返回、空态一致
4. [x] 首页双入口：两张模式卡，不再像一个工作台按钮
5. [x] `npm run build` + 写 DONE 到本文件底部

## 禁止

化学及其他学科、支付、数据库、上线、无关重构。

## DONE

- 目录：删掉三角/向量/数列等挂错题的预留叶子；函数单调性改挂 `deriv_mono`
- 教学：默认打开必修三 · 立体几何；开讲后手机可「课程目录 / 收起」
- 模型壳：教学/搜题同一套 Workspace；几何用解题顶栏返回，其它专题用专题条
- 首页：两张模式卡分别进 `/teach` `/search`
- `npm run build` 通过（2026-08-22）

还剩：三角/向量/数列真引擎；化学；搜题拍照（仍可走旧 Hub 逻辑未接到 SearchPage）
