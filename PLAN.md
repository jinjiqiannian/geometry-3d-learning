# 本机连跑：首页 + 高中数学/物理知识点全覆盖

授权：只做数学/物理前端；可改任意数量文件；不改支付/数据库；不中途提问。

## 目标

1. 首页能看懂产品：双模式、覆盖范围、真实模型，不再空/糊。
2. 教学模式覆盖高中数学、物理**全部重要知识点**（2019 人教结构）。
3. 每个知识点都有**专属模型画面**（3D 引擎或知识模型图），不是挂错题。
4. 已有引擎的点：点「开讲」进入同一套 Workspace 壳。

## 验收

- `/#/`：首屏两模式卡 + 覆盖册次/知识点数量 + 模型展示，无大段空页
- `/#/teach`：数学 必修1–2 + 选必1–3；物理 必修1–3 + 选必1–3；点任意叶子先见模型
- 有引擎的叶子能开讲；无引擎的叶子仍有公式 + 模型 + 例题步骤
- `npm run build` 通过

## 顺序

1. [x] 重写课程树（2019 人教，只留真知识点）
2. [x] 知识模型组件（按题型出图）
3. [x] 教学页：先模型课，再开讲
4. [x] 首页：覆盖区 + 收紧空白
5. [x] `npm run build`，本文件勾选

## DONE

- 课程：数学必修1–2 + 选必1–3；物理必修1–3 + 选必1–3，每个叶子有专属 `model`
- 教学：先进入模型课（公式/步骤），有引擎的再「开讲这道例题」
- 首页：覆盖区显示册次与知识点数
- 无独立引擎的点（集合、数列、平抛等）用模型课覆盖，不挂错题
- `npm run build` 通过

还剩：无引擎专题的可交互仿真（振动/波/热学等）仍是静态模型。

## 禁止

化学、支付、数据库、上线。无引擎专题不做假求解器，用模型课覆盖。
